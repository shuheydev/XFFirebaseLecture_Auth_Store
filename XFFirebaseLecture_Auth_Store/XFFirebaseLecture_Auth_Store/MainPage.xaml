<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XFFirebaseLecture_Auth_Store.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:XFFirebaseLecture_Auth_Store.ViewModels"
    mc:Ignorable="d">
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:MainViewModel x:Key="mainViewModel" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout Margin="16,32,16,0" BindingContext="{StaticResource mainViewModel}">
        <Label x:Name="greetingLabel" Text="{Binding Greeting}" />
        <Entry
            x:Name="nameEntry"
            Placeholder="Enter your name"
            Text="{Binding UserName}" />
        <Button
            Command="{Binding ClearCommand}"
            CommandParameter="{Binding UserName}"
            Text="Say hello" />
        <ListView
            x:Name="exampleListView"
            Margin="-16.0"
            ItemsSource="{Binding Contacts}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Detail="{Binding Email}" Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
